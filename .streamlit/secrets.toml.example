# .streamlit/secrets.toml
# Used by Streamlit Community Cloud for environment variables.
# Copy this to .streamlit/secrets.toml and fill in your values.
# This file is already in .gitignore — never commit the real secrets.toml

# Point Streamlit at your deployed Vercel FastAPI backend
API_BASE = "https://askfirst.vercel.app"
